To access the detection rule settings from Azure, go to Settings > Analytics. From there, you'll be redirected to Microsoft Defender, under the MS Sentinel > Settings > Analytics section.

![imagen](https://github.com/user-attachments/assets/7101c6e3-014d-4a22-8a95-8f63b22923a7)

To create the detection rule for multiple failed login attempts on our virtual machine, we will create a new schedule rule named “Multiple login failures” with the description “In the last 1 hour, multiple failed login attempts were detected from the same source IP address. This may be an indication of a brute force attack.” In my case, I set the severity to Medium and selected Technique T1110 (Credential Access - Brute Force). 

I configured the rule so that I could run tests without having to wait too long, but the thresholds can be adjusted to better suit the organization's needs. For that reason, I used the logs from the past hour and set the threshold to 5 failed attempts.

```
SecurityEvent
| where EventID == 4625
| where TimeGenerated > ago(1h)
| summarize Attempts = count(), Timestamp = max(TimeGenerated) by IpAddress, TargetAccount
| where Attempts > 5
| order by Timestamp desc
```

![imagen](https://github.com/user-attachments/assets/7963bb19-878d-4b4a-a84b-4a2ab67057a2)

In the “Alert threshold” section, configure it so that an alert is generated when the number of query results is greater than 0, and so that alerts are grouped together with all events.

For the final step, “Automated response,” we need to create a new Automation Rule, in my case, named “[AR] Multiple login failures from multiple sources”, that triggers when an incident is generated. In the “Action” section, we'll configure it to run a playbook. To link the playbook, we first need to create the playbook itself, which I've named RDP-Login-Incident.

![imagen](https://github.com/user-attachments/assets/785680ba-03d6-45d6-a64c-7ce03181367b)
